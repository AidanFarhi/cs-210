#ifndef INIT_H
#define INIT_H
#include "time.h"

/**
 * Asks user for inital values and sets them in a `Time` struct.
 * 
 * @param time The `Time` struct that will be initialized with values.
*/
void init_time(Time& time);

#endif
